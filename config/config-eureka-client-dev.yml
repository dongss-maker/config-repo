server:
  port: 9013
spring:
  application:
    name: config-eureka-client
eureka:
  instance:
    prefer-ip-address: true
    instance-id: 10.10.1.69:9013
    lease-expiration-duration-in-seconds: 10
    lease-renewal-interval-in-seconds: 5
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
person: 
  name: dongss
  sex: male
  age: 26

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
